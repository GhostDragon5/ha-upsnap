# UpSnap Home Assistant Custom Integration

A custom Home Assistant integration for UpSnap that adds device actions like wake, shutdown, reboot and sleep, plus online status monitoring via the UpSnap REST API.